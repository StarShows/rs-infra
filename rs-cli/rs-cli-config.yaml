# rs-cli Configuration
# Generated by rs-cli config
# Synced between Mac Mini (~/.rs-cli/config.yaml) and VPS (/root/.rs-cli/config.yaml)

# ── VPS Connection ───────────────────────────────────────────
ssh_host: "209.74.88.248"
ssh_user: "root"
ssh_port: "22"
ssh_key: "~/.ssh/id_ed25519"

# ── Server Info ──────────────────────────────────────────────
cpanel_user: "realtorstudio"
remote_agent_path: "/root/rs-backup-agent.sh"

# ── Local Backup Destination (Mac Mini) ─────────────────────
local_backup_root: "~/server-backups/realtorstudio.io_vps_namecheap"

# ── Retention Policy ─────────────────────────────────────────
# How many backups to keep per tier
# Every 12hrs = ~14 daily backups per week
retention_daily: 14       # ~7 days
retention_weekly: 8       # ~2 months
retention_monthly: 3      # 3 months
retention_yearly: 2       # 2 years
